LocalPick

- Frontend static files live in `Frontend/`.
- Backend API (Node + Express + MongoDB) lives in `Backend/` — see `Backend/README.md` for setup and endpoints.
- Auth pages call the API at `http://localhost:5000/api` by default.
